import { Gloss, SearchResult, Word } from "../../models/Word";
export type WordMeaningViewProps = {
    word:Word;
    searchResult:SearchResult<Gloss[]>;
}
const WordMeaningView = (props:WordMeaningViewProps) => {
    
}